# Static site deployment for satispro.net
[build]
  command = "npm run build"
  publish = "."

# Serve the static landing page for all routes
[[redirects]]
  from = "/"
  to = "/index.html"
  status = 200
